#include <stdio.h>

int main()
{
    int a,b, x1=0,x2=1,x3=0;

    printf(" Quantidade dos Termos da Sequencia de Fibonacci\n");
    scanf("%d", &a);
    for(b=1; b<a; b++)
    {
        if(b%2==1)
        {
            printf("%d, ",x3);
            x3=x1+x2;
            x1=x3;
        }
        else if(y==2)
            printf("%d, ",x3);
        else if(y%2==0)
        {
            printf("%d, ",x3);
            x3=x1+x2;
            x2=x3;
        }
    }
    printf("%d, ... ",x3);
}
